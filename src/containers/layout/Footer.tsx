import { FC, memo } from 'react';

const Footer: FC = (): JSX.Element => (
  <footer className="flex flex-grow-20 justify-center items-center bg-gray-400 text-white">
    Footer
  </footer>
);

export default memo(Footer);
